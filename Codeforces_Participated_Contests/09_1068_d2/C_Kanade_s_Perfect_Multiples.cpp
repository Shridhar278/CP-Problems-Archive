#include <bits/stdc++.h>
using namespace std;
#define int long long
#define INF (int)1e9+7
#define EPS (double)1e-9

inline void fast_io() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}

// ??????????????

int32_t main() {
    fast_io();
    int test;
    cin >> test;
    while (test--) {
        int n; cin >> n;
        int k; cin >> k;
        // vector<int> a(n);
        // vector<int> inA(k+1, 0);
        // for (int i = 0; i < n; i++) {
        //     cin >> a[i];
        //     inA[a[i]]=1;
        // }
        // sort(a.begin(), a.end());
        // vector<int> b(n);        
        // for (int i=1;i<k+1;i++) {
        //     int can = 1;
        //     for (int j=i;j<k+1;j+=i) {
        //         if (inA[j]==0) {
        //             can=0;
        //             break;
        //         }
        //     }
        //     if (can) {
        //         b.push_back(i);
        //     }
        // }

    }
    
    return 0;
}