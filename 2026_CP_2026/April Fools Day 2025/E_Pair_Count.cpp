#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, p, k;
    cin >> n >> p >> k;
    vector<int> arr(n); for (auto& x : arr) cin >> x;
    // all of this a joke leave it
    return 0;
}